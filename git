学习git笔记
使用: $ git config --global user.name "name"
      $ git config --global user.email "you@mail.com"
初始化git  在/home/username/  生成一个 .gitconfig 存放上面的name 及 mail


先建立一个文件夹，再进入此文件夹后使用$ git init 命令来始化为git仓库
ssh -T git@github.com 使用此命令来连接github
执行之后提示：Permission denied (publickey).
这说明我们还没有在GitHub账户中正确设置公钥认证，如下图所示：
建立文件(git)，
使用 git add git 
把git文件加入git索引 
再用commit提交
$ commit -a -m "修订信息"
再用
$ git remote add origin https://github.com/pl8848/test.git 
$ git push -u origin master   推送
  远程推送到github
“ 合更改需要 命令 commit -a -m 合并 ”


        #这是一个过程步骤
	git config：配置相关信息
	git clone：复制仓库
	git init：初始化仓库
	git add：添加更新内容到索引中
	git diff：比较内容
	git status：获取当前项目状况
	git commit：提交
	git branch：分支相关
	git checkout：切换分支
	git merge：合并分支
	git reset：恢复版本
	git log：查看日志
